// Generated by CoffeeScript 1.6.3
(function() {
  window.serverAddress = "http://ec2-54-218-85-114.us-west-2.compute.amazonaws.com";

  window.make_request = function(route, type, data, success, error) {
    if (type === "POST" || type === "PUT") {
      data = JSON.stringify(data);
    }
    if (success === null) {
      success = function() {
        return console.log(data);
      };
    }
    if (error === null) {
      error = function() {};
    }
    return $.ajax({
      url: serverAddress + route,
      contentType: "application/json",
      data: data,
      type: type,
      success: success,
      error: function(jqXHR, textStatus, errorThrown) {
        console.log('ERROR: ' + errorThrown);
        return raise_error_message(errorThrown + ": " + textStatus);
      }
    });
  };

  window.raise_error_message = function(error_str) {
    return $('#errors').html(error_str);
  };

}).call(this);
